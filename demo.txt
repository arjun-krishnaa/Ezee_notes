server {
#    listen 80;
#    server_name dev-stage.ezeebits.in;

    root /var/www/letsencrypt/;

    # ACME challenge location (Let’s Encrypt)
    location /.well-known/acme-challenge/ {
        allow all;
    }

    # Redirect everything else to HTTPS
    location / {
        return 301 https://$host$request_uri;
    }
}
