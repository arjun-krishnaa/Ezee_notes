firewall-cmd --permanent --add-port=8082/tcp
firewall-cmd --reload


-- Source - https://stackoverflow.com/a/1720254
-- Posted by Dan McGrath, modified by community. See post 'Timeline' for change history
-- Retrieved 2026-04-30, License - CC BY-SA 3.0

GRANT ALL PRIVILEGES ON stage_tomcat.* To 'user'@'hostname' IDENTIFIED BY 'password';




server {
    listen 80;
    server_name _;   # or your domain/IP

    location / {
        proxy_pass http://127.0.0.1:8080;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_connect_timeout 60;
        proxy_send_timeout 60;
        proxy_read_timeout 60;

        proxy_buffering off;
    }
}