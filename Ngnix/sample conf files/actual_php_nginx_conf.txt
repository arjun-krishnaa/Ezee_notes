server {
    listen       80;
    server_name  kannabiran.com;
    #rewrite ^    https://dev-cargoconsole.ezeecargo.in$request_uri? permanent;
    #include /etc/nginx/global/letsencrypt-acme-challenge.conf;
}

server {
  listen 443 ssl;
  listen [::]:443 ssl;
  #server_name dev-cargoconsole.ezeecargo.in;

  root /var/www/html/ezeeinfo/ezeeinfo;

  access_log  /var/log/nginx/portal.access.log  custom_json;

  ssl_certificate /etc/letsencrypt/live/kannabiran.com/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/kannabiran.com/privkey.pem;
  include /etc/letsencrypt/options-ssl-nginx.conf;
  ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;


  # enable session resumption to improve https performance

  # Diffie-Hellman parameter for DHE ciphersuites, recommended 2048 bits

  # enables server-side protection from BEAST attacks
  # disable SSLv3(enabled by default since nginx 0.8.19) since it's less secure then TLS
  add_header 'Referrer-Policy' 'origin';

  # ciphers chosen for forward secrecy and compatibility

  # config to enable HSTS(HTTP Strict Transport Security)


  location /logs/ {
    alias /var/log/nginx;

    autoindex on;
    autoindex_exact_size off;
    autoindex_localtime on;

    default_type text/plain;

    auth_basic "Restricted Log Access";
    auth_basic_user_file /etc/nginx/.jenkins_logs_auth;

    limit_except GET HEAD {
        deny all;
    }
  }



#  include /etc/nginx/global/mobile.conf;
#  include /etc/nginx/global/cmsportal.conf;
#  include /etc/nginx/global/bitstomcat.conf;
#  include /etc/nginx/global/letsencrypt-acme-challenge.conf;
# include /etc/nginx/global/restrictions.conf;
#  include /etc/nginx/global/php.conf;
}
